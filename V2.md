# VetSync 2.0

약물 · 시간 · mL 순으로 표시하며, 계산 근거는 작은 글씨로 붙인다.
차트에 용량이 없을 때만 기본값을 쓴다. 처치명 또는 지시사항 칸에 적힌 단위 없는 숫자 하나는 mpk로 읽는다. 다만 두 칸에 용량이 다르면 용량 불일치로 표시한다.
명시된 mL는 그대로 사용하고, mg/kg·IU/kg·µg/kg·총 mg는 단위를 맞춰 계산한다.
약물 별칭은 명시된 목록만 사용한다. 비슷한 철자만으로 약물을 추정하지 않는다.
추가 시간은 추가 준비, 사라지거나 제외된 시간은 빼기, 부피나 경로 변경은 재확인으로 표시한다.
기준은 변경 확인 전까지 유지하며, 변경 건수는 환자 단위다.

## 약물 자료

`node sync-drug-data.js`로 옆 저장소 vet_calculator/index.html의 DEFAULT_DRUGS를 내보낸다.
drug-defaults.json은 이 자료의 배포용 사본이다. 계산기는 독립 앱으로 유지한다.
dose-engine.js는 UI와 분리된 계산 모듈이다. 개인정보나 차트 자료를 포함하지 않는다.
`node build-bookmarklet.js`는 약물 자료와 모듈을 자동실행 파일에 포함한다.
브라우저 간 저장값 동기화나 실행 중 외부 약물 자료 요청은 없다.

## 복구

git 태그 vetsync-1.0.22-backup이 변경 전 전체 저장소를 보존한다.
rollback/index.html에서 이전 북마크 또는 유저스크립트를 다시 설치할 수 있다.
복구 유저스크립트는 자동 업데이트 주소를 제거했다. 확장 자체의 업데이트 정책도 확인한다.
2.0의 기준 저장 키는 v2로 분리되어 1.x 기준은 보존된다.

## 검증

node test-vetsync-panel.js
node test-vetsync-auto.js
node test-dose.js
